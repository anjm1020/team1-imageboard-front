export default ({children}) => {
    return (
        <div className="w-100 flex-grow-1 d-flex flex-row justify-content-center align-items-center flex-wrap">
            {children}
        </div>
    );
};